--- a/modules/bar/popouts/Bluetooth.qml
+++ b/modules/bar/popouts/Bluetooth.qml
@@ -18,6 +18,23 @@
     width: 300
     spacing: Tokens.spacing.small
 
+    function isMacAddressName(name: string): bool {
+        return /^([0-9A-F]{2}:){5}[0-9A-F]{2}$/i.test(name || "");
+    }
+
+    function compareDevices(a: BluetoothDevice, b: BluetoothDevice): int {
+        const priority = (b.connected - a.connected) || (b.paired - a.paired);
+        if (priority)
+            return priority;
+
+        const aIsMac = root.isMacAddressName(a.name);
+        const bIsMac = root.isMacAddressName(b.name);
+        if (aIsMac !== bIsMac)
+            return aIsMac ? 1 : -1;
+
+        return (a.name || "").localeCompare(b.name || "");
+    }
+
     StyledText {
         Layout.topMargin: Tokens.padding.medium
         Layout.rightMargin: Tokens.padding.extraSmall
@@ -62,7 +79,7 @@
 
     Repeater {
         model: ScriptModel {
-            values: [...Bluetooth.devices.values].sort((a, b) => (b.connected - a.connected) || (b.paired - a.paired) || a.name.localeCompare(b.name)).slice(0, 5) // qmllint disable unresolved-type
+            values: [...Bluetooth.devices.values].sort((a, b) => root.compareDevices(a, b)).slice(0, 5) // qmllint disable unresolved-type
         }
 
         RowLayout {
--- a/modules/nexus/pages/BluetoothPage.qml
+++ b/modules/nexus/pages/BluetoothPage.qml
@@ -17,6 +17,23 @@
     readonly property BluetoothAdapter adapter: Bluetooth.defaultAdapter // qmllint disable unresolved-type
     readonly property bool btEnabled: adapter?.enabled ?? false
 
+    function isMacAddressName(name: string): bool {
+        return /^([0-9A-F]{2}:){5}[0-9A-F]{2}$/i.test(name || "");
+    }
+
+    function compareDevices(a: BluetoothDevice, b: BluetoothDevice): int {
+        const priority = b.connected - a.connected;
+        if (priority)
+            return priority;
+
+        const aIsMac = root.isMacAddressName(a.name);
+        const bIsMac = root.isMacAddressName(b.name);
+        if (aIsMac !== bIsMac)
+            return aIsMac ? 1 : -1;
+
+        return (a.name || "").localeCompare(b.name || "");
+    }
+
     title: qsTr("Connected devices")
 
     ColumnLayout {
@@ -46,7 +63,7 @@
             placeholderText: root.btEnabled ? qsTr("No saved devices") : qsTr("Bluetooth disabled")
 
             model: ScriptModel {
-                values: Bluetooth.devices.values.filter(d => d.bonded).sort((a, b) => (b.connected - a.connected) || a.name.localeCompare(b.name)) // qmllint disable unresolved-type
+                values: Bluetooth.devices.values.filter(d => d.bonded).sort((a, b) => root.compareDevices(a, b)) // qmllint disable unresolved-type
             }
 
             delegate: StyledRect {
--- a/modules/nexus/pages/bluetooth/BluetoothPairing.qml
+++ b/modules/nexus/pages/bluetooth/BluetoothPairing.qml
@@ -16,6 +16,23 @@
 
     readonly property BluetoothAdapter adapter: Bluetooth.defaultAdapter // qmllint disable unresolved-type
 
+    function isMacAddressName(name: string): bool {
+        return /^([0-9A-F]{2}:){5}[0-9A-F]{2}$/i.test(name || "");
+    }
+
+    function compareDevices(a: BluetoothDevice, b: BluetoothDevice): int {
+        const priority = b.pairing - a.pairing;
+        if (priority)
+            return priority;
+
+        const aIsMac = root.isMacAddressName(a.name);
+        const bIsMac = root.isMacAddressName(b.name);
+        if (aIsMac !== bIsMac)
+            return aIsMac ? 1 : -1;
+
+        return (a.name || "").localeCompare(b.name || "");
+    }
+
     function setScan(on: bool): void {
         if (adapter?.enabled)
             adapter.discovering = on;
@@ -74,7 +91,7 @@
             list.anchors.top: scanIndicator.bottom
 
             model: ScriptModel {
-                values: Bluetooth.devices.values.filter(d => !d.bonded).sort((a, b) => (b.pairing - a.pairing) || a.name.localeCompare(b.name)) // qmllint disable unresolved-type
+                values: Bluetooth.devices.values.filter(d => !d.bonded).sort((a, b) => root.compareDevices(a, b)) // qmllint disable unresolved-type
             }
 
             delegate: Item {
