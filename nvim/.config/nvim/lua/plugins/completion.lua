return {
  {
    "saghen/blink.cmp",
    opts = {
      keymap = {
        -- Tab both opens the menu and accepts the selection.
        -- Shift-Tab jumps backwards inside snippets.
        -- With this preset, <Enter> goes back to being a plain newline.
        preset = "super-tab",
      },
    },
  },
}
